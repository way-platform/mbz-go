edition = "2023";

package wayplatform.connect.mercedesbenz.vehiclespec.v1;

// Credentials for Mercedes-Benz Vehicle Specification API (API key).
message Credentials {
  // API key for vehicle specification endpoints.
  string api_key = 1 [debug_redact = true];
}
